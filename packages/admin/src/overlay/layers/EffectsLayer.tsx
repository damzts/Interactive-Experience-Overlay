/** EffectsLayer — reserved for global effects not tied to a scene source.
 *  Scene-level CRT/FX are rendered through the plugin system in LayerStack.
 *  Kept as a mount point for future global shaders / post-processing. */
export function EffectsLayer() {
  return null
}
