/**
 * @module components
 *
 * Root barrel file for the IEOM Admin Panel component library.
 * Provides a single entry point for importing any component from the
 * design system, organized by atomic design categories:
 * - Atoms: smallest building blocks (Button, Input, Toggle, etc.)
 * - Molecules: composed from atoms (Card, Field, Toast, etc.)
 * - Organisms: complex UI sections (Sidebar, Modal, ConfigPanel, etc.)
 */

export * from './atoms';
export * from './molecules';
export * from './organisms';
